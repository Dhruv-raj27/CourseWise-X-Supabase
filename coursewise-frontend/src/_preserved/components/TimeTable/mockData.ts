// Temporarily disabled to fix TypeScript errors
export const days: string[] = [];
export const timeSlots: string[] = [];

export const formatTimeSlot = (slot: any): string => {
  return '';
};

export const to24Hour = (time: string): string => {
  return '';
};

export const mockCourses: any[] = [];

export const checkTimeClash = (course1: any, course2: any): boolean => {
  return false;
};

export const doTimeSlotsClash = (slot1: any, slot2: any): boolean => {
  return false;
}; 